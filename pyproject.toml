[project]
name = "elc-sem4-secure-network-config"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "argparse",
    "numpy",
    "matplotlib",
]
